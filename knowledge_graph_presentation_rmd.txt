---
title: "Knowledge Graph-Enhanced Hate Incident Classification"
subtitle: "Scaling Intelligence in an Age of Exponential Hate"
author: "Randy Howk - CUNY DATA 607 Final Project"
date: "`r Sys.Date()`"
output:
  ioslides_presentation:
    widescreen: true
    smaller: false
    css: styles.css
  slidy_presentation: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)

library(tidyverse)
library(jsonlite)
library(knitr)
library(kableExtra)
library(DiagrammeR)
```

## The Original Vision {.build}

**Initial Proposal**
- Build a predictive model to classify hate incidents  
- Train on historical ADL incident data  
- Predict categories for new incidents  
- Focus: *Machine learning classification accuracy*

<div class="notes">
Traditional ML approach – train model, evaluate accuracy, deploy
</div>

## The Pivot: A Critical Realization {.build}

```{r load-data}
incidents <- read_csv("output/structured_incidents.csv", show_col_types = FALSE)
glossary  <- read_csv("data/processed/glossary_final.csv", show_col_types = FALSE)

n_glossary  <- nrow(glossary)
n_incidents <- nrow(incidents)

incidents_formatted <- format(n_incidents, big.mark = ",")
```

### The Scale Problem

::: {style="font-size: 24px; margin-top: 30px;"}
| Resource | Growth Rate | Current Count |
|---------|-------------|---------------|
| ADL Glossary | Slow (curated) | `r n_glossary` terms |
| Hate Incidents | Exponential | `r incidents_formatted` incidents/year |
:::

<div class="centered" style="margin-top: 40px; font-size: 28px; color: #d9534f;">
**The glossary can't keep up with the incidents**
</div>

## The Urgent Need {.build}

<div style="font-size: 22px; margin-top: 20px;">
- Bondi Beach, Australia – Rising antisemitic incidents  
- Redlands, California – Synagogue attacks  
- Amsterdam, Netherlands – Post-soccer violence against Israeli fans  
- Global surge – Incidents up 360% since October 2023  
</div>

<div class="centered" style="margin-top: 50px; padding: 20px; background-color: #fcf8e3; border-left: 5px solid #f0ad4e;">
<span style="font-size: 24px; font-weight: bold;">
We can't wait years to manually catalog every new hate term.<br>
We need to process thousands of incidents NOW.
</span>
</div>

## The Solution: Knowledge Graph Approach {.build}

<div style="font-size: 20px;">
Instead of traditional ML:
- Use the glossary as a semantic knowledge base  
- Encode ADL expert-curated terminology as embeddings  
- Build a knowledge graph connecting terms, categories, and relationships  
- Match incidents via semantic similarity  
- Leverage expert intelligence at machine scale  
</div>

<div class="centered" style="margin-top: 40px; font-size: 24px; color: #5cb85c;">
**Result:** Expert intelligence × Machine speed = Scalable processing
</div>

## The Pipeline: 7 Phases

```{r pipeline-diagram}
grViz("
digraph pipeline {
  rankdir=LR;
  node [shape=box, style=filled, fontsize=10];

  P1 [label='1. Data\nIngestion', fillcolor='#d9edf7'];
  P2 [label='2. Glossary\nProcessing', fillcolor='#d9edf7'];
  P3 [label='3. Embedding\nGeneration', fillcolor='#dff0d8'];
  P4 [label='4. Knowledge\nGraph', fillcolor='#dff0d8'];
  P5 [label='5. Incident\nProcessing', fillcolor='#fcf8e3'];
  P6 [label='6. Evaluation', fillcolor='#f2dede'];
  P7 [label='7. Export\nfor Analysis', fillcolor='#f2dede'];

  P1 -> P2 -> P3 -> P4 -> P5 -> P6 -> P7;
}
")
```

<div style="text-align: center; font-size: 18px; margin-top: 20px;">
**16,893 historical incidents** → **1,142 glossary terms** → **9,603 new incidents classified**
</div>

## Key Innovation: Semantic Matching

**Traditional**
```
IF incident.text CONTAINS "swastika"
THEN category = "Numbers / Symbols"
```

**Knowledge Graph**
```
embedding_similarity(incident, glossary_terms)
→ top_match: "swastika" (0.87)
→ category: "Numbers / Symbols"
→ context + related concepts
```

<div style="margin-top: 30px; padding: 15px; background-color: #d4edda; border-left: 5px solid #28a745;">
**Captures meaning, not just keywords**
</div>

## The Results: Machine vs Human {.columns-2}

### Category Granularity

```{r comparison-table}
data.frame(
  System = c("Human", "Machine"),
  Categories = c(1, 23),
  Information = c("Broad", "Granular")
) %>%
  kable(align = c("l", "c", "l")) %>%
  kable_styling(font_size = 20)
```

<div style="margin-top: 20px; font-size: 24px; color: #5cb85c; font-weight: bold;">
Machine provides 23× more detailed intelligence
</div>

## What That Granularity Means

```{r top-categories}
top_cats <- incidents %>%
  count(predicted_category_machine_granular, sort = TRUE) %>%
  head(8)

ggplot(top_cats, aes(x = reorder(predicted_category_machine_granular, n), y = n)) +
  geom_col(fill = "steelblue", alpha = 0.8) +
  geom_text(aes(label = scales::comma(n)), hjust = -0.1, size = 5) +
  coord_flip() +
  labs(title = "Machine Identifies Specific Threat Types",
       x = "", y = "Number of Incidents") +
  theme_minimal(base_size = 14)
```

## Confidence Scoring

```{r confidence-hist}
ggplot(incidents, aes(x = confidence)) +
  geom_histogram(bins = 50, fill = "steelblue", alpha = 0.7, color = "black") +
  geom_vline(xintercept = 0.85, linetype = "dashed", size = 1.2) +
  geom_vline(xintercept = 0.50, linetype = "dashed", size = 1.2) +
  labs(title = "Confidence Enables Intelligent Automation",
       x = "Confidence Score", y = "Number of Incidents") +
  theme_minimal(base_size = 14)
```

## Key Achievements {.build}

::: {style="font-size: 22px;"}
- End-to-end automated pipeline (7 phases)  
- Knowledge graph with 21,594 nodes and 28,394 relationships  
- 100% data completeness  
- 23× more granular classification  
- Confidence-based automation  
- Production-ready and scalable  
:::

## The Bottom Line {.centered}

<div style="font-size: 32px; margin-top: 60px; line-height: 1.8;">
In a world where hate incidents are surging…<br>
<span style="color: #d9534f; font-weight: bold;">
Manual processing cannot scale
</span><br><br>
<span style="color: #5cb85c; font-weight: bold;">
Expert Intelligence × Machine Speed = Scalable Action
</span>
</div>

## Questions? {.centered}

<div style="margin-top: 100px; font-size: 24px;">
Project Repository:<br>
/Users/randyhowk/Documents/CUNY/607/FinalProject/Cassidy
</div>
